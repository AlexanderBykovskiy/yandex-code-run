module.exports = function (mapString) {

    const rows = mapString.split("");
    const rCount = mapString.length;

    const timeInSec = Infinity;

    return timeInSec; // первая секунда, в которую в кастрюле отсутствуют буквы
}
