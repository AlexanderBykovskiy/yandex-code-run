module.exports = function (inputStr) {

    if (inputStr.length < 5) return '';

    const lib = ["-----", ".----", "..---", "...--", "....-", ".....", "-....", "--...", "---..", "----."];

    inputStr += " ";
    outputStr = "";
    const len = inputStr.length;

    let word = "";

    for (let i = 0; i < len; i++ ) {

    }

    return inputStr;
}
